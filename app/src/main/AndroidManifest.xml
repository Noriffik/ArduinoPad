<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dev.aproschenko.arduinocontroller" >

    <uses-sdk android:minSdkVersion="17"/>

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        >

        <activity
                android:name="com.dev.aproschenko.arduinocontroller.MainActivity"
                android:label="@string/app_name"
                android:configChanges="orientation"
                android:screenOrientation="portrait">
                >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
                android:name="com.dev.aproschenko.arduinocontroller.DeviceControlActivity"
                android:parentActivityName="com.dev.aproschenko.arduinocontroller.MainActivity"
                android:configChanges="orientation"
                android:screenOrientation="portrait">
                >
        </activity>

        <activity
            android:name="com.dev.aproschenko.arduinocontroller.TerminalActivity"
            android:parentActivityName="com.dev.aproschenko.arduinocontroller.DeviceControlActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
            >
        </activity>

    </application>

</manifest>
